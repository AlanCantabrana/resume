import styled from 'styled-components'

const CertificationContainer = styled.section`
    display: grid;
    grid-template-columns: 1fr 1fr;
`
const ContentWrapper = styled.div`
    width: 100%;
`
export { CertificationContainer, ContentWrapper }
